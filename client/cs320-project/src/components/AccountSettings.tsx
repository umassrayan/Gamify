const AccountSettings = () => {
  return (
    <div style={{ padding: "40px" }}>
      <h1>Account Settings</h1>
      <p>This page fully replaces the previous one.</p>
    </div>
  );
};

export default AccountSettings;
